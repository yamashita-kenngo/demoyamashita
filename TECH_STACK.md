# プロジェクト概要

## 技術スタック

### Cloud Functions for Firebase を活用した イベント駆動アーキテクチャ

このプロジェクトで採用されているイベント駆動アーキテクチャ (Event-Driven Architecture, EDA) は、システム内で発生する重要な「イベント」を核として処理を進める設計アプローチです。具体的には、Firestore へのデータ書き込み、Firebase Authentication でのユーザー作成、クライアントからの API リクエスト (`onCall`) といった出来事（イベント）をトリガーとして、Cloud Functions for Firebase がバックエンドの処理を実行します。

これは、Google Cloud が示すイベント駆動アーキテクチャの考え方に基づいています。イベントの発生源（プロデューサー）、イベントの検知と伝達（ルーティング）、そしてイベントに応じた処理の実行（コンシューマー）という要素で構成されます。
([参考: イベント ドリブン アーキテクチャ | Google Cloud](https://cloud.google.com/eventarc/docs/event-driven-architectures?hl=ja))

#### このプロジェクトにおける主要コンポーネント

1.  **イベントプロデューサー (Event Producers)**
    * イベント（出来事）を生成する起点となるサービスやアクションです。
    * **Firestore**: データベースのドキュメントが作成、更新、削除された場合にイベントが発生します。
    * **Firebase Authentication**: 新規ユーザーが作成されたり、ユーザーが削除されたりした場合にイベントが発生します。
    * **クライアントアプリケーション**: `onCall` 関数を介して HTTPS リクエストを送信することで、API呼び出しというイベントが発生します。
    * **Cloud Storage for Firebase**: ファイルがアップロード、更新、削除された場合にイベントが発生します。

2.  **イベントルーティング (Event Routing)**
    * 発生したイベントを、それを処理すべき Cloud Function へと結びつける仕組みです。
    * **Firebase Background Triggers**: Firestore (`onWrite`, `onCreate` など) や Authentication (`onCreate`, `onDelete` など) のイベント発生を Firebase が検知し、対応する Cloud Function を自動的に起動させます。これは内部的に Google Cloud のイベント処理基盤を利用しています。
    * **HTTPS Triggers (`onCall`)**: クライアントからの HTTPS リクエストを直接受け付け、指定された Cloud Function を起動します。

3.  **イベントコンシューマー (Event Consumers)**
    * イベントを受け取り、具体的なビジネスロジックやデータ処理を実行するコンポーネントです。
    * **Cloud Functions for Firebase**: このプロジェクトの中心的なコンシューマーです。各イベントに応じて、データの加工、他の Firebase サービスへの書き込み、外部 API との連携、通知の送信といった様々なバックエンド処理を実行します。

#### 処理フローの例 (Firestore イベント)

1.  **[プロデューサー]** ユーザーがモバイルアプリから操作を行い、Firestore 内の特定のドキュメントデータが更新されます。
2.  **[ルーティング]** Firebase バックエンドがこの Firestore ドキュメントの更新イベント (`onUpdate`) を検知します。事前に定義されたトリガー設定に基づき、このイベントを処理する Cloud Function へ情報がルーティングされます。
3.  **[コンシューマー]** 対象の Cloud Function が起動し、更新されたデータを受け取ります。関数は、例えば「関連する別のドキュメントのカウンターを更新する」「特定の条件を満たした場合に管理者に通知を送る」といったロジックを実行します。

#### イベント駆動アーキテクチャのメリット

* **疎結合 (Loose Coupling)**: イベントを発生させるサービス (例: Firestore) と、それを処理する関数が直接的な依存関係を持たないため、各コンポーネントを独立して開発、デプロイ、更新しやすくなります。
* **スケーラビリティ (Scalability)**: Cloud Functions は受け取ったイベントの量に応じて自動的にスケールアップ・ダウンします。これにより、トラフィックの増減に柔軟に対応できます。
* **応答性 (Responsiveness)**: イベントが発生すると非同期的に処理を開始できるため、ユーザーへの応答を待たせずにバックグラウンドでタスクを実行でき、システム全体の応答性が向上します。
* **拡張性 (Extensibility)**: 新しいイベント処理ロジックが必要になった場合、既存のシステムに影響を与えずに新しい Cloud Function を追加するだけで対応できます。

このように、Cloud Functions for Firebase を活用することで、Firebase エコシステム内で完結した、スケーラブルで柔軟なイベント駆動型のバックエンドシステムを構築できます。

### 言語とランタイム

-   **Node.js**: ランタイム環境としてv20 LTSが使用されます。
    -   **Cloud Functions対応**: Cloud Functions 第2世代はNode.js 20をサポートしています。
        -   参考: [Google Cloud Functions 実行環境 - Node.js](https://cloud.google.com/functions/docs/concepts/execution-environments?hl=ja#nodejs)
    -   **v20 ライフサイクル**: Node.js v20はLTS（長期サポート）バージョンであり、サポート終了 (EOL) は **2026年4月30日** の予定です。
        -   参考: [Node.js 公式リリーススケジュール](https://nodejs.org/en/about/releases/)
-   **TypeScript**: 主要なプログラミング言語として使用されます。

### テスト

-   **インテグレーションテスト**:
    -   **Firebase Emulator**: Firebaseサービスとのローカルなインテグレーションテストに使用されます。
-   **Jest**: ユニットテストに使用されます。

### リンティングとフォーマット

-   **Biome**: コードのリンティングとフォーマットに使用されます。

### トランクベース開発 (Trunk-Based Development) によるブランチ戦略

トランクベース開発は、ソフトウェア開発におけるブランチ管理戦略の一つです。このアプローチでは、開発者全員が `main` (または `master`, `trunk` と呼ばれることが多い) という単一の共有ブランチ（トランク）を開発の主軸とし、ここに頻繁に小さな変更を直接統合（コミット＆プッシュ）していきます。

#### 中心的な原則

* **単一のトランク**: `main` ブランチが常に最新かつ安定したコードベースを表します。開発のメインラインはこのトランクのみです。
* **頻繁な統合 (Continuous Integration)**: 各開発者は自身の変更を少なくとも1日に1回、理想的にはそれ以上頻繁にトランクへ統合します。これにより、コードの乖離（かいり）を最小限に抑えます。
* **短命なブランチ**: 機能開発やバグ修正のためにブランチを作成する場合でも、それらは非常に短期間（通常は数時間から最大でも数日）でトランクにマージされ、すぐに削除されます。長期間存続するフィーチャーブランチは原則として使用しません。

#### 主要な実践プラクティス

1.  **`trunk` への直接コミット**: 小さく独立した変更は、レビュー後などに直接 `trunk` へコミットされます。
2.  **継続的インテグレーション (CI) の徹底**: `trunk` へのすべてのコミットは、自動ビルドと包括的な自動テスト（ユニットテスト、インテグレーションテストなど）をトリガーします。これにより、`trunk` が常に健全でデプロイ可能な状態に保たれます。
3.  **フィーチャーフラグ (Feature Flags / Toggles)**: 未完成の機能やリリース前の機能を `trunk` にマージするために使用されます。フラグを使って機能の有効/無効を制御することで、未完成のコードが本番環境に影響を与えることなく統合を進められます。
    * **Firebase Remote Config を利用した管理**:
        Firebase Remote Config は、アプリのアップデートをユーザーに要求することなく、アプリの動作や外観を変更できるクラウドサービスです。これをフィーチャーフラグの実装に効果的に活用できます。
        * **パラメータ定義**: Firebase コンソール上で、機能の ON/OFF を制御するパラメータ（例: `feature_new_dashboard_enabled`, 値: `true`/`false`）をキー・バリュー形式で定義します。
        * **値の取得**: アプリケーション（クライアントアプリや、Admin SDK を使用するバックエンド関数）は起動時や実行時に Remote Config からこれらのパラメータの値を取得します。
        * **条件付き有効化**: アプリケーションは取得した値に基づいて、特定の機能へのアクセスを制御します。例えば、`feature_new_dashboard_enabled` が `true` の場合にのみ新しいダッシュボードを表示するように実装します。
        * **段階的ロールアウトとターゲティング**: Remote Config の「条件」機能を利用して、特定のアプリバージョン、国、言語、ユーザープロパティ、オーディエンス、またはランダムなパーセンテージのユーザーに対してパラメータ値を設定できます。これにより、新機能を一部のユーザーに限定して公開（カナリアリリースやA/Bテスト）したり、段階的に全ユーザーへ展開したりすることが可能です。
        * **動的な制御**: アプリの再デプロイなしに、Firebase コンソールからパラメータ値を変更するだけで、機能の有効/無効をリアルタイム（または次回アプリ起動時）に切り替えられます。

        このように Firebase Remote Config を使うことで、コード自体は `trunk` に統合済みであっても、実際の機能リリースや挙動の制御は Firebase コンソールから柔軟に行うことができ、トランクベース開発における機能管理と安全なリリースが容易になります。
4.  **リリースブランチ (必要な場合)**: 本番リリース直前に、特定のコミットポイントから短命なリリースブランチを作成することがあります。このブランチでは、バグ修正などリリースに必要な最小限の変更のみを行い、安定化させます。開発自体は引き続き `trunk` で行われます。

#### 開発フローの例

1.  開発者は作業開始前に必ず `trunk` の最新状態を取得します (`git pull origin main`)。
2.  ローカルでコードの変更とテストを行います。
3.  変更が完了したら、再度 `trunk` の最新状態を取得し、ローカルでマージ（またはリベース）して競合がないか確認します。
4.  変更を `trunk` にプッシュします (`git push origin main`)。
5.  CI/CD パイプラインが自動的にビルド、テスト、場合によってはデプロイを実行します。
6.  CI で問題が検出された場合は、担当者が迅速に修正し、再度プッシュします。

#### メリット

* **コード統合の簡素化**: 長期間分岐したブランチのマージに伴う複雑さや大規模な競合（"Merge Hell"）を回避できます。
* **迅速なフィードバック**: CI により、コードの問題が早期に発見され、修正コストが低減します。
* **継続的デリバリー (CD) の実現**: `trunk` が常にリリース可能な状態に保たれるため、頻繁なリリースや継続的デリバリーが容易になります。
* **チーム内の透明性向上**: 全員の作業が常に `trunk` に統合されるため、プロジェクト全体の進捗状況が把握しやすくなります。

#### 考慮事項・前提条件

* **高品質な自動テスト**: `trunk` の安定性を保証するためには、広範囲をカバーする信頼性の高い自動テストが不可欠です。
* **堅牢な CI/CD パイプライン**: 頻繁な統合とテストを高速に処理できる CI/CD 環境が必要です。
* **フィーチャーフラグの適切な管理**: フィーチャーフラグの数が増えると管理が複雑になるため、計画的な運用と、Firebase Remote Config のような管理ツールの活用が重要です。フラグの命名規則やライフサイクル管理（不要になったフラグの削除など）も考慮が必要です。
* **開発者の規律**: 頻繁な統合とテストの維持、小さなコミットの徹底など、チーム全体での規律が重要です。

トランクベース開発は、特にアジャイルな開発プロセスや DevOps の文化と親和性が高く、ソフトウェアのリリースサイクルを高速化したいチームに適した戦略です。Firebase Remote Config のようなツールを組み合わせることで、より安全かつ柔軟に開発を進めることができます。

### コード管理における注意事項

このプロジェクトにおいて、コードの品質、安全性、および開発効率を維持・向上させるためには、以下のコード管理プラクティスに従うことが重要です。特に GitHub を利用したトランクベース開発 (TBD) を実践する上で、これらの点は不可欠となります。

#### 1. Git と GitHub の利用規約

* **コミットメッセージ**:
    * 変更内容とその目的が明確に伝わる、意味のあるコミットメッセージを記述してください。([Conventional Commits](https://www.conventionalcommits.org/) のような規約に従うことを推奨します)。
    * 1つのコミットは、関連する変更のみを含む論理的な単位（アトミックコミット）にしてください。
* **トランクベース開発 (TBD) の遵守**:
    * 開発の主軸は常に `main` ブランチです。変更は頻繁に `main` ブランチへ統合（プッシュ）してください。
    * フィーチャーブランチを使用する場合:
        * ブランチは極力短命（理想は1日以内）に保ち、作業完了後すぐに `main` へマージしてください。
        * マージ後は不要になったブランチを速やかに削除してください。
        * `main` へのマージには、コードレビューと CI チェックを経るためにプルリクエスト (Pull Request) を使用することを推奨します。
* **`main` ブランチの保護**:
    * GitHub のブランチ保護ルールを設定し、`main` ブランチへの直接プッシュを制限したり、CI チェックやコードレビューの完了をマージの必須条件としたりしてください。
    * Force Push (`git push --force`) は原則禁止とします。
* **.gitignore の徹底**:
    * `node_modules/`, `.env`, ビルド成果物、ログファイル、OS 固有ファイルなど、リポジトリで管理すべきでないファイル/ディレクトリは `.gitignore` に正確に記述してください。

#### 2. コード品質の確保

* **リンティングとフォーマット (Biome)**:
    * `biome.json` で定義されたルールにコードが準拠していることを常に確認してください。
    * コミット前、または CI プロセスで Biome によるチェックとフォーマットを必ず実行してください。
* **テスト (Jest, Firebase Emulator)**:
    * 新しい機能や修正には、必ず対応するユニットテスト (Jest) やインテグレーションテスト (Firebase Emulator) を作成・更新してください。
    * `main` ブランチにコードを統合する前に、ローカルおよび CI 環境で全てのテストが成功することを確認してください。
    * テストカバレッジの維持・向上を目指してください。

#### 3. 開発プロセスとコラボレーション

* **コードレビュー**:
    * `main` ブランチへのすべての変更（プルリクエスト経由）に対して、チームメンバーによるコードレビューを実施してください。品質向上と知識共有のために建設的なレビューを心がけてください。
* **コミュニケーション**:
    * 特に TBD では、他のメンバーとの作業競合を避けるため、現在取り組んでいるタスクや大きな変更について、事前にチーム内でコミュニケーションを取ってください。
* **課題管理 (GitHub Issues 等)**:
    * バグ報告、機能要望、タスク管理には GitHub Issues などを活用し、状況を追跡可能にしてください。

#### 4. 依存関係の管理

* **バージョン固定**:
    * `package-lock.json` を必ずリポジトリにコミットし、開発者間および CI/CD 環境で依存パッケージのバージョンを統一してください。これによりビルドの再現性を担保します。
* **脆弱性チェック**:
    * `npm audit` や GitHub Dependabot などを利用して、依存パッケージに既知の脆弱性がないか定期的にチェックし、発見された場合は迅速に対応してください。
* **定期的な更新**:
    * 依存関係は定期的に見直し、セキュリティパッチや改善が含まれるアップデートを適用することを検討してください。ただし、互換性の破壊がないかテストが必要です。

#### 5. セキュリティ対策

* **機密情報の漏洩防止**:
    * API キー、パスワード、証明書ファイルなどの機密情報は、絶対にコードリポジトリにコミットしないでください。
    * 環境変数、`.env` ファイル (ただし `.gitignore` に追加)、Firebase Functions 環境設定、Google Secret Manager、GitHub Secrets 等を利用して安全に管理してください。
* **セキュアコーディングの実践**:
    * Cloud Functions では、入力値の検証、エスケープ処理、適切な認証・認可チェックを必ず実装してください。
    * Firebase セキュリティルール (Firestore, Realtime Database, Storage) は、最小権限の原則に基づき、適切かつ厳密に設定してください。
* **定期的な診断**:
    * 可能であれば、静的解析セキュリティテスト (SAST) ツールや、定期的なセキュリティ診断を実施してください。

#### 6. ドキュメント整備

* **README.md**:
    * プロジェクトの目的、技術スタック、セットアップ方法、実行・テスト方法、デプロイ手順などを `README.md` に記載し、常に最新の状態を維持してください。
* **コード内ドキュメント**:
    * 複雑なアルゴリズム、公開 API、設定項目など、理解が難しい箇所には JSDoc/TSDoc 形式でコメントを記述してください。
* **設計・アーキテクチャ文書**:
    * 重要な設計判断やアーキテクチャ概要に関するドキュメント（必要であれば図を含む）を維持・更新してください。

#### 7. CI/CD パイプラインの活用

* **自動化**:
    * リンティング、フォーマット、テスト、ビルド、デプロイといったプロセスを CI/CD パイプラインで自動化し、手作業によるミスを減らし、一貫性を保ってください。
* **フィードバック**:
    * CI パイプラインは、コードの問題を早期に発見するための重要なフィードバックメカニズムです。パイプラインが常に安定稼働するようにメンテナンスしてください。
* **デプロイ戦略**:
    * 本番環境へのデプロイは、`main` ブランチの安定したコミットからのみ、かつ CI/CD の全ステージが成功した場合にのみ実行されるように構成してください。

これらの注意事項をチーム全体で遵守することで、コードの品質を維持し、安全かつ効率的な開発サイクルを実現できます。